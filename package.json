{
  "name": "bookdock",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:desktop": "pnpm --filter @bookdock/desktop dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
